{
  "short_name": "{{=it.config.then(config => config.title)}}",
  "name": "{{=it.config.then(config => config.title)}}",
  "description": "{{=it.config.then(config => config.description)}}",
  "theme_color": "{{=it.config.then(config => config.theme.themeColor)}}",
  "background_color": "{{=it.config.then(config => config.theme.backgroundColor)}}",
  "icons": [
    {
      "src": "/images/app-192.png",
      "type": "image/png",
      "sizes": "192x192"
    },
    {
      "src": "/images/app-512.png",
      "type": "image/png",
      "sizes": "512x512"
    }
  ],
  "start_url": "/",
  "display": "standalone"
}